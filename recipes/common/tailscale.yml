---
modules:
  - type: default-flatpaks
    configurations:
      - scope: system
        notify: false
        install:
          - org.fkoehler.KTailctl
  - type: rpm-ostree
    install:
      - tailscale
