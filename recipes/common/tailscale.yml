---
modules:
  - type: default-flatpaks
    configurations:
      - scope: system
        notify: false
        install:
          - org.fkoehler.KTailctl
  - type: dnf
    install:
      packages:
        - tailscale
